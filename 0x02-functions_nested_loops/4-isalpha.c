#include <stdio.h>
#include <stdlib.h>
#include<string.h>
#include <time.h>
#include <ctype.h>
/* more headers goes there */
int main(void);
int _isalpha(int c)
{
if (isalpha(c))
{
return (1);
}else
{
return (0);
}
}
